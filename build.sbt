
lazy val root = (project in file(".")).settings(
	name := "Latek",
	version := "1.0",
	scalaVersion := "2.10.3"
)

//scalacOptions += "-feature"

//scalacOptions += "-deprecation"

//target := file("../build")

//assemblyJarName in assembly := "latek.jar"

//assemblyOutputPath in assembly := file("../build/latek.jar")
